// Use "code ." in terminal to open preferred IDE for a particular folder.

function sayHello(name) {
    console.log('Hello ' + name);
};

sayHello('Sean');

// Use "node 'JavaScript file name'" in terminal to run preferred file. Ex.. "node app.js"

